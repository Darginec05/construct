# Self-host server the editor's Publish button targets. Copy to `.env` and set.
# When unset, Publish is disabled (the editor stays a pure sandbox).
VITE_CONSTRUCT_SERVER_URL=http://localhost:8787

# Only needed if the server was started with CONSTRUCT_API_KEY.
# VITE_CONSTRUCT_API_KEY=secret
